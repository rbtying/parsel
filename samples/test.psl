main(input[char], data[char]) -> (signal, signal) =
    let signal ss = if data == "hi"
                    then s
                    else 2 * s
        signal s = signalFromWav(input)
    in (ss, ss)
